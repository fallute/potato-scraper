services:
  - type: worker
    name: potato-scraper
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python scrape_all.py
    plan: free
